# DrunkAgent — 复现发现与批判性分析

本文回答四个复现问题，并从代码出发给出对论文优缺点的解读。它结合了 (a) 对论文算法与
所报告数值的静态分析，和 (b) 一份**实际运行过的**重新实现（见 `repro/`，结果在
`results/`）。

> **诚实的边界。** 论文的原始技术栈无法在本沙箱运行（HuggingFace + OpenAI 被网络
> 封锁、无 GPU）。我用 Claude 顶替了各个 LLM 角色，用合成 CD 数据集顶替 Amazon。
> 因此绝对数值天然与论文不同；能迁移过来的是方法的**定性行为**。下文每一条经验性
> 论断都可以通过 `python3 -m repro.run` 和 `python3 -m repro.control_injectability`
> 复现。

---

## 四个问题

### Q1 — 哪里复现不了？

1. **在本环境里端到端地讲：没有任何东西能按论文规格跑起来** —— Llama-3-8B 代理模型
   （白盒 NLL）、gpt-4-turbo 生成 LLM、GPT-o1 防御、GPT-Neo perplexity，以及
   Amazon/Yelp 数据集全部被封。忠实运行需要 GPU + 可访问的 HF/OpenAI。
2. **贪心搜索的打分（Eq. 4）在没有白盒代理模型时无法按原文复现。** 它是对 agent 生成
   的排序文本做 token 级 NLL；只能用 API 的受害者（Claude、GPT-4）拿不到 logprob，
   于是精确目标无法计算。我替换为黑盒倒数平均排名（MRR）打分。任何用闭源模型复现的人
   都会撞上同一堵墙。
3. **停止判据定义不足。** Algorithm 1 第 3 行在攻击"稳定达成"时停止 —— 没有阈值。
   第 16 行循环"直到恶意动作发生为止"，这使得 Table 6 的 100% 是**构造出来必然成立的**
   （见 Q4）。
4. **不确定 / 会漂移的依赖。** gpt-4-turbo 和 GPT-o1 随时间漂移；`temperature=0,
   seed=2024` 只钉住了受害者推理，钉不住 GPT-4 的生成调用。即便满配访问，精确数值也
   不可复现。

### Q2 — 哪个 workload 掉点最多？

从论文自己的 Table 1 看，攻击在以下情形明显更弱：
- **Musical Instruments**（每个受害者都是）：DrunkAgent HR@1 = 0.2268 / 0.1340 /
  0.1443（CF/RAG/SEQ），而 **CDs & Vinyl** 是 0.4040 / 0.3131 / 0.4949 —— 大约
  掉了 3×。
- **AgentRAG** 是跨数据集最鲁棒的受害者；最弱的单格是 AgentRAG × Musical
  Instruments（0.1340），比摘要倚重的头条 CDs 数字低约 3.7×。

我的复现重现了这个**方向**：见"经验结果"表 —— AgentRAG 是最难攻的受害者，效果是
**集中**的，而非均匀的。

### Q3 — 哪个假设不稳？

**承重且最脆弱的假设是：受害者 LLM 会服从嵌入在物品描述里的指令（即"策略模块"）。**
该策略就是教科书式的 prompt injection：`Ignore previous instructions`、
`### NEW TASK BEGINS ###`、`Task complete!!!!!!`、`!!!!!!`。它是否奏效，完全取决于
受害者后端对注入的易感性 —— 而论文从未改变这一点（单一后端族；代理 ≈ 受害者，Table 4）。

我的控制实验（`control_injectability.py`）固定攻击，只翻转受害者的易感性：
- 在一个**服从注入**的后端上，策略生效；
- 在一个**有戒备**的后端上（把描述当数据），同样的注入**毫无作用** —— 而且加上它反而
  会**拉低**目标排名，因为 `### NEW TASK ###!!!!!!` 这类文本读起来像垃圾信息。

所以论文的核心新意其实并非"agentic 记忆腐蚀" —— 它是 prompt injection，其成败押在一个
关于受害者的脆弱假设上。

次要的不稳假设：**代理→受害者迁移。** Table 4 显示受害者的提示模板与代理几乎一致，
所以所报告的"黑盒可迁移性"更接近白盒模板迁移。

### Q4 — 哪个指标对不上 / 撑不住？

1. **Table 6"灌醉目标 agent 的成功率"处处 100%。** 这是循环论证：Algorithm 1 第 16 行
   循环*直到*恶意动作发生，所以在代理模型上 100% 是构造出来的。它对真实受害者什么都
   没说。在我的运行里，对有戒备受害者的同类"灌醉成功率"**远低于 100%**（见表）——
   该指标在面对一个抗注入模型时无法存活。
2. **Perplexity（Fig. 5）。** 绝对值（45–637）依赖打分器/分词器，且论文对打分器自相
   矛盾（正文说 GPT-Neo；设置部分说"标准 perplexity [43]"）。它还是量在流畅的
   *trigger* 上，而非真正被注入的那段刺眼的*策略*文本 —— 也就是说隐蔽性指标和被注入的
   载荷根本不是同一个对象。
3. **HR/NDCG 绝对值。** 我验证过 Table 1 里每个数值都等于 k/N，其中 N≈97–99（被
   下采样的用户数）。评测建立在 ~99 个用户、且看起来是单个采样目标项之上，没有方差/
   置信区间。一个用户就能让指标变动约 1 个点；什么都复现不到四位小数。
4. **消融方向（Table 5）是后端特定的。** 论文：去掉策略攻击就崩（策略是必需的）。我在
   有戒备后端上的运行：去掉策略反而*更好*，而只留策略 = 0。定性的消融结论会随受害者而
   反转。

---

## 核心优点（从方法看，而非从包装看）

- **一个真正新颖且现实的威胁面：** 编辑物品自身的描述 —— 卖家本就合法拥有的能力 ——
  来操纵一个 agentic 推荐器。其"促销"（把某一项顶到第 1）的设定贴合真实的金钱驱动型
  滥用，且易于度量。
- **两路并进的设计是合理的：** 一个可迁移的有说服力 trigger（用贪心文本搜索优化）加上
  一个指令注入策略。
- **"通过代理模型做黑盒"在概念上是正确的设置。**

## 核心缺点（从代码/数字看）

- **它在机制上就是 prompt injection。** 论文自己的 Table 5 显示策略模块在干活；我的
  运行显示，在一个鲁棒模型上注入无用/有害，攻击退化为"把文案写得更好看"（一次普通的
  ChatGPT 改写就能追平）。
- **隐蔽性被夸大且自相矛盾。** 被注入的载荷含有 `### NEW TASK ###`、
  `Ignore previous instructions`、`!!!!!!` —— 一行正则就能抓出来（我的检测器会标记
  DrunkAgent，且从不标记 Benign）。低 perplexity 报的是另一段（仅 trigger）的字符串。
- **防御评测薄弱：** 只测了改写（他们自己也承认有时反而*增强*攻击）；没有指令注入
  检测器、分隔符/角色隔离、或记忆写入校验 —— 也就是说，没测那些直接克制 prompt
  injection 的防御。
- **统计单薄且可能被高估：** ~99 用户、~1 个目标项、无方差；代理 ≈ 受害者；Table 6
  的 100% 是构造出来的。
- **可复现性会衰减：** 钉死在会漂移的闭源端点（gpt-4-turbo、GPT-o1）和一个特定开源
  模型上，且停止判据定义不足。

---

## 经验结果（实际运行；Claude 后端，20 用户，合成 CD 数据）

### 可迁移性 —— 按受害者与攻击分的 HR@1

| 攻击 | AgentCF | AgentRAG | AgentSEQ |
|---|---|---|---|
| Benign | 0.000 | 0.050 | 0.000 |
| TrivialInsertion | 0.000 | 0.050 | 0.050 |
| ChatGPTAttack（普通 LLM 改写） | 0.150 | 0.150 | 0.050 |
| **DrunkAgent（完整 = trigger + 策略）** | 0.150 | **0.000** | **0.000** |
| DrunkAgent — 仅 trigger（去注入） | **0.250** | **0.400** | **0.350** |
| DrunkAgent — 仅注入（去 trigger） | 0.000 | 0.000 | 0.000 |

如何读这张表：
- **注入（"策略"）从不帮忙，且通常摧毁攻击。** 仅 trigger 在每个受害者上都打赢完整
  DrunkAgent（0.25 对 0.15；0.40 对 0.00；0.35 对 0.00）。仅注入处处是 0.000。这与
  论文 Table 5 **正好相反**（那里策略是必需组件）。
- **真正有效的部分只是有说服力的文案。** 仅 trigger 是最强的攻击，而一次普通的
  ChatGPT 改写（无注入、无贪心搜索）就已经追平"完整 DrunkAgent"。
- **AgentRAG/AgentSEQ 是鲁棒的受害者**（DrunkAgent 0.000），与论文的鲁棒性排序一致 ——
  但在这里 DrunkAgent 在它们身上输给了普通改写。

### "灌醉成功率"（完整 DrunkAgent 下目标被排到第 1）—— 对比论文 Table 6 = 100%

| AgentCF | AgentRAG | AgentSEQ |
|---|---|---|
| 0.150 | 0.000 | 0.000 |

论文在每个数据集/预算上都报 100%。面对一个抗注入受害者，这是 0–15%。那个 100% 是代理
模型上"优化到成功为止"停止规则的产物，并非真实受害者的属性。

### 隐蔽性 / 可检测性（用一行正则匹配注入标记）

| Benign | TrivialInsertion | ChatGPTAttack | DrunkAgent（完整） | 仅 trigger | 仅注入 |
|---|---|---|---|---|---|
| 否 | 否 | 否 | **是** | 否 | **是** |

那个"隐蔽"的完整攻击是可被轻易检测的（它含有 `### NEW TASK ###`、
`Ignore previous instructions`、`!!!!!!`）。唯一既隐蔽*又*有效的变体是仅 trigger ——
也就是"把文案写得更好"，而这毫无新意。

### 控制实验：攻击是不是只需要一个可注入的受害者？（`control_injectability.py`）

同一个 DrunkAgent 攻击、同一个 AgentCF 受害者，只改变后端遵循指令的"纪律性"：

| 攻击 | guarded HR@1 | obedient HR@1 |
|---|---|---|
| Benign | 0.000 | 0.000 |
| ChatGPTAttack | 0.000 | 0.250 |
| DrunkAgent — 仅 trigger | 0.250 | 0.250 |
| DrunkAgent — 仅注入 | 0.000 | 0.000 |
| DrunkAgent — 完整（trigger+注入） | 0.000 | 0.000 |

解读：说服（trigger / 改写）能改变排序；而**注入在两种受害者配置下都改变不了任何东西
（0.000）**，并且会*毒化*那个本来有效的 trigger（仅 trigger 0.250 → 完整 0.000）。值得
注意的是，**即便系统提示明确告诉 Claude 去服从物品描述里的指令，它依然抵抗注入** ——
也就是说，我无法构造出一个能被论文策略模块攻破的 Claude 受害者。因此论文的高分看起来
是其更弱、更易注入的开源受害者（Llama-3-8B-Instruct 族，且代理 ≈ 受害者）所特有的。
"agentic 记忆腐蚀"机制经不起更换后端。

### 复现的总结论

在一个抗注入的后端上，**DrunkAgent 的招牌机制发生反转**：prompt-injection 的"策略
模块"—— 论文的核心新意 —— 无用或有害，攻击退化为有说服力的文案写作（一次 GPT 改写
即可追平），100% 的灌醉成功率站不住，载荷被一行正则抓出。论文里该方法之所以成功，最好
的解释是它的受害者（单一、相对易注入的开源模型族，且代理 ≈ 受害者），而不是什么新的
"agentic 记忆"漏洞。
